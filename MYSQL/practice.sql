-- create table Users1(user_id int primary key auto_increment,full_name varchar(100) not null,email varchar(100) unique not null,city varchar(100) not null,registration_date date not null);
-- desc Users;
-- create table  Events1(event_id int primary key auto_increment,title varchar(200) not null,decription text not null,start_date DATETIME not null,end_date DATETIME not null,status enum('upcoming','complelted','cancelled'),organizer_id int ,foreign key(organizer_id) REFERENCES Users(user_id));
-- desc Events1;
-- create table Sessions1(session_id int primary key auto_increment,event_id int,title varchar(100) not null,speaker_name varchar(100) not null,start_time datetime not null,end_time datetime not null,foreign key(event_id) references Events1(event_id));
-- desc Sessions1;
-- create table Registrations1(registration_id int primary key auto_increment,user_id int,event_id int,registration_date date not null,foreign key(user_id) references Users1(user_id),foreign key(event_id) references Events1(event_id));
-- desc Registrations1;
-- create table Feedback1(feedback_id int primary key auto_increment,user_id int,event_id int,rating int check(rating between 1 and 5),comments text,feedback_date date not null,foreign key(user_id) references Users1(user_id),foreign key(event_id) references Events1(event_id));
-- desc Feedback1;
-- create table Resources1(resource_id int primary key auto_increment,event_id int,resource_type enum('pdf','image','link'),resource_url varchar(25) not null,uploded_at datetime not null,foreign key(event_id) references Events1(event_id));
-- desc Resources1;
-- insert into Users1 select * from Users;
select * from Users1;
-- insert into Events1 select * from Events;
-- insert into Sessions1 select * from Sessions;
-- insert into Registrations1 select * from Registrations;
-- insert into Feedback1 select * form Feedback;
-- insert into Resources1 select * form Resources;